# AI Software Team Frontend

This directory contains a React + Vite frontend for the AI Software Team project.

## Setup

```bash
cd ui/frontend
npm install
npm run dev
```

## API

The frontend talks to the local backend at `http://localhost:8000`.
